# security-sanity-checker
What it does: a tiny Python CLI that checks a list of inputs/files for basic security issues like weak password patterns, suspicious URLs, exposed secrets in .env-style text, and simple log anomalies. This is short, practical, and aligned with your Python + cybersecurity path. Password strength checker.
